# Spring Boot Configuration
server.port=8080
spring.application.name=bank-core-agent

# Logging
logging.level.root=INFO
logging.level.com.smancode.smanagent=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# Business Graph Configuration
business.graph.project-path=/Users/liuchao/projects/opencode
business.graph.auto-analyze=false

# CORS Configuration
cors.allowed-origins=http://localhost:*
cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
cors.allowed-headers=*
